---
title: Payment History and Reconciliation
description: >-
  Track, query, and reconcile agent payment history using gRPC queries,
  checkpoint subscriptions, and custom indexer pipelines.
keywords:
  - payment history
  - reconciliation
  - transaction history
  - checkpoint subscription
  - indexer
  - audit
goal:
  description: >-
    Reader can query agent payment history and reconcile local records
    against onchain transaction data
  requires:
    - has_frontmatter:
        - title
        - description
        - keywords
      label: Has required frontmatter fields
    - min_words: 300
      label: Needs more content depth
    - pattern: 'ImportContent'
      min: 1
      label: Has code examples
    - has_questions: true
      label: Needs questions for AI search visibility
    - has_answer: true
      label: Needs answer summary for AI citation
builder_paths:
  - path_id: agentic-payments
    path_name: Agentic Payments
    step: Payment reconciliation
    stage: Operations
questions:
  - How do I track payment history for a Sui agent?
  - How do I reconcile agent payments on Sui?
  - How do I detect missed or duplicate payments on Sui?
answer: >-
  List the agent's transactions by sender with the gRPC client's
  listTransactions method, then compare local payment records against
  onchain balance changes to detect missed, duplicate, or failed payments.
---

Autonomous agents can lose track of payments due to crashes, network timeouts, or retry failures. Payment reconciliation compares the agent's local payment records against onchain transaction history to detect missed, duplicate, or failed payments.

## Querying transaction history

List the agent's finalized transactions with the gRPC client's `listTransactions` method, filtered by sender:

<ImportContent source="examples/onchain-finance/payment-reconciliation/src/reconcile.ts" mode="code" tag="list-transactions" />

Results are cursor-paginated in ledger order. Persist the returned cursor between reconciliation runs and pass it as `after` so each pass scans only transactions added since the last one.

## Verifying individual payments

A digest in the sender's history proves the transaction is final; it does not prove the payment succeeded or paid the right party. Fetch the transaction's balance changes and match them against the payment the agent intended to make:

<ImportContent source="examples/onchain-finance/payment-reconciliation/src/reconcile.ts" mode="code" tag="verify-payment" />

Balance changes report each address's net change per coin type, so a payment transaction also produces entries that are not payments: the sender's own debit and storage rebates credited back to the sender. Matching on the expected recipient and coin type avoids misclassifying those artifacts as payments.

## Real-time monitoring with checkpoint subscriptions

The gRPC `SubscriptionService.SubscribeCheckpoints` streams checkpoints as the network finalizes them. Filter the stream to checkpoints that contain the agent's transactions and feed each matching transaction into the reconciliation state:

<ImportContent source="examples/onchain-finance/payment-reconciliation/src/reconcile.ts" mode="code" tag="subscribe-checkpoints" />

Subscriptions always begin at the current tip of the chain and do not support resumption. Persist the last received cursor; after a disconnect, re-subscribe first and note the new stream's first cursor, then replay the gap between the two with `LedgerService.ListCheckpoints` (same filter, `startCheckpoint` of the persisted cursor + 1). Replaying before re-subscribing silently misses checkpoints that finalize between the end of the replay and the new stream's first frame.

For event-level filtering — for example, only payment events emitted by a specific Move module — use `SubscriptionService.SubscribeEvents`, which accepts a filter over event type, emitting module, or sender. For high-volume production monitoring, use a [custom indexer pipeline](/develop/accessing-data/custom-indexer) built on the `sui-indexer-alt` framework, which processes checkpoints and writes matching events to your database.

## Reconciliation algorithm

Compare the agent's local payment records against the sender's finalized onchain history:

<ImportContent source="examples/onchain-finance/payment-reconciliation/src/reconcile.ts" mode="code" tag="reconcile-algorithm" />

Run the reconciliation pass on a schedule and alert on discrepancies that persist across runs.

## Production reconciliation

For production agents processing high volumes, use a [custom indexer](/develop/accessing-data/custom-indexer) built on the `sui-indexer-alt` framework. The indexer continuously processes checkpoints and writes payment data to your database, giving you a complete, queryable history without polling the RPC.

See [Accessing Data](/develop/accessing-data) for the full overview of data access options.
